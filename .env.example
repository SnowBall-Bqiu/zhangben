# Server
NODE_ENV=production
HOST=0.0.0.0
PORT=3000
DATA_DIR=/app/data

# Set to true only when the site is served over HTTPS.
SESSION_COOKIE_SECURE=false

# Required in production. Generate a long random value before deployment.
SESSION_SECRET=change-this-session-secret
